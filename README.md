# Project name: **component-observer**

### Annotations

> The annotations you may have stumbled upon by examining the source code, as follows:..

```diff
    DEV_NOTE # ... : a note left by author developer
    DEV_NOTE (!) # ... : the IMPORTANT note left by author developer
```

### Table of contents

> _Coming soon..._

### Development environment

#### **PREREQUISITES**:

- Make sure you got the latest `Node.js` and `npm` on your machine, as well as the dependendies  as per `package(-lock).json` description;

#### **REMARKS**:

This is rather a conceptual model where a lifecyle methods of `global-db` web component will align well with a concept of updating `IndexedDB` version itself, or removing the instance of database via `global-db` reference using standard DOM methods, such as `globalDB.remove()`.

---

> Made with ♥ by [**projektorius96**](https://github.com/projektorius96).